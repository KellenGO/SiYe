# 更新记录

## 未发布（发版时改成 `vX.Y.Z · 日期`）

### 新增

- **本地收藏夹图标模式**：保留原列表，可切到封面卡片网格浏览「全部收藏」、默认收藏夹、稍后再看与自建夹；偏好记在本机。点卡片仍进入原有可编辑内容列表，空夹与图片加载失败统一用浅色底占位（封面格不放占位图标）。

- **B站、知乎平台收藏夹只读浏览**：跨平台收藏页会把已同步的收藏夹显示为封面卡片网格；空夹也可见，点开后按本机分页浏览，返回会保留卡片页位置。平台收藏夹的名称与归属不可在四野编辑，仍可把内容另存到自己的本地收藏夹。

- 收藏同步的分页共用层已支持页码、offset 与字符串 cursor：知乎收藏夹目录/内容分页、小红书收藏笔记流接入全量导入与轻量更新；每页和原始检查点令牌同事务保存。抖音全量导入暂不启用，等待可靠的当前账号识别接口。

- **B站收藏不限条数（实验性）**：B站收藏不再受 100 条上限，改为逐页读取**全量**并存到本机，中断后能接着读、按账号分开保存；列表与详情分开，不逐条补详情。其他平台仍最多 100 条。
- **一键同步改为增量**：再次同步只拉新增——从最近收藏往后读，读到一段本机已有的内容就停，不用每次重走一遍完整列表。需要覆盖本机跨平台收藏时，从高级操作使用「重置同步」。
- **收藏后当场改归属**：在搜索结果里点收藏，卡片下方会弹出和收藏页同款的信息条（收藏时间 + 编辑归属 + 添加备注），不用再跑到收藏页去找那条。
- **每条收藏都能一键彻底删除**：过去只能进批量管理删除，现在收藏页每条、以及搜索结果里刚收藏完的那条，都能直接删掉（居中确认框）。

### 改进

- **搜索结果与观看历史更容易返回顶部**：两页向下滚动一小段后就显示原有的右下角回顶按钮；收藏页原有显示时机不变。
- **跨平台收藏同步分为日常与重置**：「同步所选平台 / 继续」只新增；「重置同步」收在高级操作中，旁边只用一句话说明覆盖本机归档，确认后只替换成功完整读取的所选平台本机归档。失败、未登录或取消的未完成平台保留旧归档，个人本地收藏和平台上的收藏不受影响。
- **收藏、热搜与登录说明更清楚**：跨平台收藏说明改为「只读取，不改动平台收藏」；小红书热搜在切换前就标明暂不可用，点开可看原因；帮助页默认收起可选的浏览器扩展安装步骤，扫码登录仍是主流程。
- **教程会直接指出该点哪里**：讲到「认识首页」这一步时，页面会压暗、把「搜索范围」那一行框出来，旁边贴一句短提示。框跟着页面滚动走，窗口大小变了也贴得住；它**只是提示，不挡操作** —— 高亮的地方和页面其它地方都照样能点。教程进行中右下角多了一个「退出教程」，滚到哪儿都关得掉（和在卡片里点「这次跳过」是一回事，不影响「以后不再自动提示」的设置）。
- **新手引导扩为七步逐页导览**：不再只讲账号、搜索、收藏、帮助，而是把首页、搜索结果、结果卡片上的操作、本地与跨平台收藏、观看历史、外观与搜索数量设置挨个走一遍，一步落到一个真实页面。帮助页新增「页面与功能一览」作为常驻的逐页说明，教程页脚会把你指过去；教程随时可以跳过或从帮助里重开。
- **V1.0 界面改成普通用户语言**：收藏页不再展示接口、分页和数据库等实现说明；日常同步与重置同步分别说明。账号页移除本地 API、浏览器后端、Cookie 计数、错误码和命令行提示，只保留当前状态、下一步操作与必要的安全说明；完整排障信息仍可通过帮助页的诊断报告提供。
- **本地收藏去掉「未分类」视图**：列表侧栏与图标网格两处都不再提供入口——用户不需要这个分类。后端 `stats.unclassified` 与 `GET /items?unclassified=true` 作为数据能力保留，不再有任何界面入口。
- **「取消收藏」和「彻底删除」合并成同一个动作**：原来一个只是解绑默认收藏夹、另一个才是删，现在点「取消收藏」就是整条移除（含备注与所有归属），点之前弹确认框，可勾「下次不再提示」；只想换收藏夹请用「编辑归属」。
- **「全部收藏」不再包含「稍后再看」**：只挂在稍后再看里的内容不会混进「全部」；「稍后再看」是独立的一条线，只由它自己的按钮控制，归属面板里不再出现它。
- **收藏页保持熟悉的样子**：平台勾选 → 一键同步 → 平台页签点着切。结果一次拿全，但界面一次只渲染 100 条，底部「显示更多」逐次展开——收藏攒到上千条也不会卡。
- 搜索结果与收藏页的序号统一成品牌色数字 + 上方短横线。
- 搜索框里「编辑归属」「添加备注」的排布与收藏页一致；外观设置页的出口从页面底部移回顶栏——首页右上角「自定义」进来，同一位置就是「回首页」。

### 修复

- **V1 窄屏与长列表稳定性**：搜索、本机收藏和观看历史统一每 100 条逐批显示；390px 收藏页的视图/备份按钮可自然换行，顶部通知不再盖住按钮；图标视图刷新后保持；320px 设置分类不再被裁掉。
- **平台勾选严格按你上一次的选择记住，包括「一个都不勾」**：原先取消掉最后一个平台时，存储里还留着旧值，刷新后那个平台会自己勾回来（你并没有勾它）。现在勾选状态就是你最后一次点的样子：勾了哪个记住哪个，全取消也能记住。平台搜索失败被自动取消勾选后，也不会在刷新时「复活」。
- **搜索页的平台勾选不再「往返一次就变全选」**：平台按钮默认一个都不勾、由你自己勾，但此前从首页进「设置」再回到搜索页（或刷新）会变成四个平台全勾。现在空偏好读回来仍是零勾选；存储损坏时仍兜底全选，不会把你卡住。
- 知乎文章、回答等不同类型的同数字 ID 现在以类型加 ID 隔离，避免跨平台收藏内容或收藏夹归属串在一起；历史已被覆盖的数据保留现状，无法凭空恢复。
- 收藏同步状态中的数量改为「本次读取并保存」，不再暗示新增量；每夹保留脱敏的分页/停止原因诊断，便于后续解释同步结果。

- B站收藏分页的中间短页不再被误判为列表变化；按接口的 `has_more` 继续读取，已经收到的页会先保存。增量更新改为逐收藏夹连续命中 5 条历史内容后转下一个收藏夹，完成的收藏夹立即获得自己的下次同步基线；单个收藏夹异常会保留进度并继续其余收藏夹。

- 搜索框的光标停在最左边时被输入框自身的圆角裁成一段弧线。
- 收藏下拉里的「全部加入收藏」按钮被压成方块、文字竖排（`34×34` 的规则误伤了面板里的文字按钮）。
- 取消收藏的确认框过窄、按钮不显眼，且默认焦点落在确认按钮上（回车就会删）——现在放宽、按钮做实底红、焦点默认在「取消」。
### 说明

- 日常同步**只看头部**：从最近收藏往后读，读到一段本机已有的内容就停，所以你在平台上删掉的**旧内容**不会被发现，本机仍会留着。想用平台当前可读取的列表覆盖本机跨平台收藏时，到高级操作使用「重置同步」。
- 增量的前提是"B站按收藏时间从新到旧返回"。这一点还没在你自己的账号上实测过：2026-09-21 那次只读探测因为登录状态失效没跑成。以后如果发现新收藏没同步进来，先怀疑这里（`scripts/probe_bilibili_favorites.py` 能验证，只读不写库）。
- 平台没有一致性快照，"这次没出现"**不等于**你取消了收藏，所以日常同步不会因为"没看到"而删除本机内容；用户确认「重置同步」后才会覆盖所选且成功读取平台的本机归档。

## v0.3.0 · 2026-09-20

### 新增

- 新增**热搜卡片**：首页按平台切换展示各平台热搜词（已接入抖音 / B站 / 知乎），点一条直接用当前勾选的平台搜索它；取代了原来的「最近搜到」板块。热搜只在进入软件时取一次，之后只有你点「刷新」才更新。
- 搜索范围那栏新增**单平台重搜**（每颗平台胶囊右边的 ⟳，平台卡片上的「重试」同源）：只重搜这一个平台，拿到的是**与上一轮不重复的新内容**，替换它在当前页里的位置，其它平台一条都不动、**不会多出一批**；没搜过的平台点它就是把它补进来。区别于「换一批」（往后叠加新内容）与「刷新结果」（整组重搜）。
- 结果页新增**「← 返回首页」**（挂在搜索框左侧外面、主题色圆角按钮，**不会挤压搜索框**）：回首页**不会清掉**上次搜索，首页会给一个「查看上次搜索」入口切回来，两页可以来回看；首页的平台那排不再显示单独重搜按钮（只在结果页出现）。

### 改进

- 平台**没搜到会说清原因**：0 条与失败同等对待 —— 弹页面上方居中的提示卡片、说明原因并自动取消勾选；抖音账号/IP 被风控时显示「疑似平台风控」，未登录导致的空结果直接提示去登录。平台状态条也会显示这条原因，不再是光秃秃的「无结果」。
- 「返回首页」只切视图：不清结果、不清任务、不动平台勾选。

### 修复

- 极少数情况下平台**明明成功了却显示「搜索失败」**：worker 报完成功就退出、最后那行结束标记没被读到时会误判，现在以平台自己报过的终态为准，已拿到的结果不会被翻成失败。
- 单平台重搜后**其它平台的结果不再消失**，连续重搜多个平台也不会把会话弄断（此前会出现"重搜一个平台，别的平台内容没了"）。
- 平台**空的搜索结果也不再谎称**"已自动取消勾选"：只有确实勾着的平台才会被取消。
- 排障能力：worker 子进程的日志尾部会在平台没结果/失败时打到后端控制台（已脱敏，不含凭据），平台"搜不出来"时能直接看到平台返回的形状。

## v0.2.3 · 2026-09-18

### 新增

- 版本号收敛到单一来源（运行时读 `base/app_version.py`），帮助页显示当前版本，发版时改一处即可；打包用的 `pyproject.toml` 与前端 `webui/package.json` 由测试盯着对齐。
- 新增**观看历史**：点开链接看过的内容会自动记下来，在顶部「历史」里按最近浏览倒序查看，可删单条或清空；只保留最近 1000 条。它与收藏完全独立，不会混进收藏夹。
- 新增**主题色**：「设置 · 外观与首页」里可选 8 种色（湛蓝 / 石墨 / 松绿 / 青碧 / 紫藤 / 玫瑰 / 酒红 / 赭橙）；主题色只改强调色，深浅仍由浅色 / 深色开关单独控制。

### 改进

- 账号页每张卡的主结论改成一句话「可用 / 不可用 · 原因」，并给出下一步该点的按钮；同步过程、诊断、平台能力等具体原因全部收进默认折叠的「详情」，不再平铺。
- 账号页补上两处盲区：本机浏览器不可用会在顶部提示，并说明抖音允许公开搜索（未登录也可能出结果，但不算登录证明）。
- 搜索时某个平台没搜到，改为页面上方居中的蓝色弹窗，几秒后自动消失，说明原因与「已自动取消 xxx 的勾选」，未登录时可直接跳去登录；结果区不再挂常驻黄卡。
- 平台勾选改为首次进入不默认全选，由用户自己勾选想要的平台；教程明确说明这四个按钮是勾选框而不是平台状态灯，并补上结果内关键词筛选在排序下拉旁（原来写成了在导出面板里）。

### 说明

- 平台正在冷却（限流后 60 秒起）这件事仍然只在搜索结果页出现：账号页目前拿不到冷却数据，需要后端在账号接口补字段才能显示，本轮没做。

## v0.2.2 · 2026-09-16

### 新增

- 增加 Windows x64 安装器：按当前用户安装、无需管理员权限，提供开始菜单入口、可选桌面快捷方式、覆盖升级和标准卸载；便携 ZIP 继续保留。
- 增加首次使用引导：用户可以跟随应用内步骤完成账号连接、搜索和收藏，也可以暂时跳过、永久关闭，并随时从帮助页重新打开。
- 帮助页增加本地诊断报告，可预览、复制或下载经过白名单筛选的运行状态，方便提交问题且不包含 Cookie、搜索词和收藏内容。
- 本地收藏增加固定的「默认收藏夹」和「稍后再看」，结果卡可独立切换，收藏页可统一编辑和批量管理归属。

### 改进

- 历史记录和推荐搜索词改为只填入搜索框，不再自动搜索或覆盖当前平台勾选；新手教程补充平台勾选、取消和至少保留一个平台的说明。
- 登录提示改为优先连接 1–2 个常用平台，不再让首次使用者误以为必须一次登录全部平台。
- README、使用说明、推广页和 GitHub 问题模板统一到新的上手与反馈路径。
- 收藏数据库迁到 `%LOCALAPPDATA%\SiYe\data`，启动时自动备份并合并旧源码和发行目录中的收藏与跨平台缓存；未登录或部分同步不再让历史内容看似消失。
- 跨平台收藏状态分别显示本次获取数与本机保留数。
- 本地收藏列表强化单条内容与其收藏时间、归属和备注的视觉分组；归属卡中的超长收藏夹名会省略显示，不再撑出卡片或页面。
- 互动指标只合并、不倒退：一次超时、限流或只拿到列表字段的同步，不会再覆盖掉以前已经取到的完整指标。
- 收藏详情补全改为两个带节流的槽位，B站 100 条能在预算内补完（原来是串行 + 2 秒间隔 + 120 秒上限，数学上只能补约 50 条）；预算用尽不再把整个平台判成同步超时。
- 指标缓存跟着收藏库放在一起，源码版与发行版共用，换版本不必重新读取详情。
- 顶部品牌区在「四野」字标左侧加入应用 icon，与窗口图标保持一致。

### 说明

- 抖音与小红书的浏览量仍然拿不到（列表接口不返回，也不去试探未证实可用的接口），本次未做补全。

## v0.2.1 · 2026-09-15

### 改进

- 产品与仓库名称统一为「四野 / SiYe」：GitHub 仓库更名为 `KellenGO/SiYe`，
  使用说明与推广页地址变为 <https://kellengo.github.io/SiYe/guide.html>，
  发布包为 `SiYe-Windows-x64.zip`。旧地址会自动跳转，已收藏的链接无需更换。
- 启用前端代码检查（eslint）：此前 `npm run lint` 是个悬空脚本，
  依赖里没有 eslint，检查从未真正跑过。
- 持续集成的后端任务补装 Node：此前依赖 Node 的同步协议用例会被静默跳过，
  结果显示为通过但并未执行。
- 发布包校验补上子目录里的用户数据检查，进一步避免误打包本机收藏与登录态。

### 说明

- 本版本不含功能改动，只做命名统一与工程收尾。

## v0.2.0 · 2026-09-15

### 新增

- 本地收藏库支持收藏夹分类、备注编辑、备份导入与导出。
- 跨平台收藏支持按平台同步，每个平台最多读取 100 条，并将同步结果保存在本地。
- 跨平台收藏同步可随时取消；已保存的内容会保留，下一次主动同步再更新。
- 增加应用内扫码登录，减少对浏览器扩展的依赖。
- 桌面端增加托盘启动器，双击 `四野.exe` 时后台启动服务，不显示命令行窗口。
- 搜索结果支持渐进返回、排序、筛选、跨平台去重、详情展开、收藏和 CSV／Markdown 导出。

### 改进

- 统一四个平台的账号状态、登录验证和搜索前检查，避免旧状态覆盖新结果。
- 统一平台颜色、结果指标图标、封面与长文本展开行为。
- 合并重复的 worker 生命周期、平台 HTTP 客户端和账号操作守卫，减少维护分叉。
- 清理未使用的代理目录、依赖、组件和重复启动脚本。
- 更新中英文界面文案、使用说明和项目介绍页。

### 说明

- 本版本仍是 Windows x64 桌面端，账号、收藏和缓存默认只保存在本机。
- 项目采用仓库中的非商业学习使用许可证；请在法律与平台规则允许的范围内使用。
- 真实平台登录、搜索和同步结果会受账号状态、平台接口和限流影响。
