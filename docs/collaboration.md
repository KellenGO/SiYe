# 当前协作状态

开工先按 [AGENTS.md](../AGENTS.md) 核对 Git 与相关 [功能 wiki](index.md)。本页只记录当前写入归属和下一位接手者仍需知道的事实；不是任务日志，也不是文件锁。已完成的普通任务以提交、功能 wiki 和 CHANGELOG 为准。

## 当前写入任务

只登记正在修改文件、暂停后仍占用范围，或等待集成的任务。只读评估无需登记。一个 worktree 同时只有一个写入负责人；登记不能替代核对实际状态。完成并提交后删除本行，接手风险未消除的再写入下方。

| 任务 ID | 负责人 / 会话 | worktree / 分支 / 起始 HEAD | 修改范围 | 状态 / 下一步 |
|---|---|---|---|---|
| `V1-RELEASE-20260924` | Codex / 当前发布任务 | `C:/Users/Kellen/Desktop/MediaCrawler-main` / `master` / `5a3f5f5` | `docs/collaboration.md`；隔离构建 worktree 的生成文件 | 核对发布门槛；取得实机与真实账号验收后才推送 tag。 |

## 待接手事项

仅保留未集成、未完成、阻塞、归属不明的改动，或代码和 wiki 难以重建的验证限制。每项用任务 ID 或提交定位，写清现状、下一步和负责人；解决后删除。不替其他会话宣布完成。

- `V1-RELEASE-READINESS-20260924`：本地 `master` 的 v1.0.0 发行包通过 EXE 与安装器 clean-room；本次不推送、不打 tag、不发布。当前提交的 GitHub Actions 尚未运行；本机用 Python 3.12 构建，工作流指定 3.11。发布执行者需在实际发布前核对该提交的 CI 结果，并按功能 wiki 完成托盘窗口/菜单及真实平台账号的人工实机验收。构建产物仅在本地忽略的 `dist/`。

这不证明其他 worktree 或未登记的会话没有工作。接手前仍要看 `git worktree list`、各相关 worktree 的 status 与最新提交。

## 留记录的边界

- 功能当前行为与边界写对应 `docs/features/`，长期取舍写 `docs/decisions/`，用户可感知版本变化写 `CHANGELOG.md`。这里不复制改动清单、测试输出、界面文案或完整调试过程。
- 需要交接时，短记「任务 ID / 提交或分支、当前状态、关键发现、验证及未验证、下一步与归属」。仅涉及运行效果时说明源码、构建产物与正在运行的版本是否一致。
- 支线 worktree 的本文件是独立副本。以 `git worktree list` 确认主工作区路径；主工作区有写入负责人时由其串行更新公共记录，支线先在自身提交中保留任务 ID，集成者再汇总尚未解决的事项。无法确认公共记录时先做只读检查。
- 旧的完整交接仍可从 Git 查看：`git show 01b4453:docs/collaboration.md`；按任务 ID 搜提交可用 `git log --all --grep=<任务ID>`。不为普通已完成任务另建归档或重复写交接。
