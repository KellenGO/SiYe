# 远端收藏夹镜像边界

## 背景

跨平台归档已有内容和归属，但页面无法按真实远端收藏夹浏览。

## 决定

在同一 SQLite 中保存只读远端目录，身份为账号与远端夹 ID；内容继续复用既有归档与归属表。目录成功与内容扫描成功分开记录。小红书单一收藏流不冒充多夹，抖音在可验证账号身份缺失时不启用镜像。

## 后果

本地可编辑收藏夹与远端镜像不互相导入或修改。前端浏览和知乎类型身份迁移仍待完成。

状态：进行中
